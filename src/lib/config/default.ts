const configuration = {
  routes: [
    { title: 'home', link: '/' },
    { title: 'sign-up', link: '/auth/sign-up' },
    { title: 'sign-in', link: '/auth/sign-in' }
  ]
};

export default configuration;
